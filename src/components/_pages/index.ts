import styled from "styled-components";

export const Main = styled.main`
  padding: 2.5rem 0;
  display: grid;
  gap: 3rem;
`;

export const EnterpiseList = styled.div`
  display: grid;
  gap: 2rem;
`;
